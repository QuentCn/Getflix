(function ($) {
    ('use strict');

    $(".menu-toggle").on("click", function () {
        else.preventDefault();
        $("sidebar-wrapper").toggleClass("active");
        $("")
    })
})
    (jQuery);